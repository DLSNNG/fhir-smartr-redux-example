import React from 'react'
import PropTypes from 'prop-types'

const LoadingSpinner = () => {
  return (
    <div className="loading-spinner">
      I am a loading spinner...
    </div>
  )
}

export default LoadingSpinner